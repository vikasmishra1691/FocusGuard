<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FocusGuard" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.FocusGuard" parent="Base.Theme.FocusGuard" />

    <!-- No Action Bar theme for blocking activities -->
    <style name="Theme.FocusGuard.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
